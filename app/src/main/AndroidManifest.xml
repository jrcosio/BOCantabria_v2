<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Avisos (feature 012). Se pide en contexto, tras guardar el primer aviso; nunca al arrancar. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".BOCantabriaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BOCantabria.Starting">
        <!--
          El bloqueo vertical es una decisión de producto explícita del propietario: la aplicación
          no tiene utilidad en horizontal en un móvil. Se suprimen los dos avisos de lint a
          conciencia:
            - LockedOrientationActivity avisa de que se limita al usuario, que es lo pretendido.
            - DiscouragedApi avisa de que a partir de Android 16 se ignora en pantallas grandes.
              Es cierto y está aceptado: en tablets no se intentará forzar (research.md, D-011).
        -->
        <!--
          Compartir el PDF exige una content:// con permiso temporal de lectura: pasar un file://
          lanza FileUriExposedException desde Android 7. `exported="false"` y `grantUriPermissions`
          es la combinación que da acceso solo a quien recibe el Intent, y solo a ese fichero.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.documents"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
          WorkManager se inicializa desde Koin (`workManagerFactory()`), para que el Worker de avisos
          reciba sus dependencias por constructor. El inicializador por defecto arrancaría antes que
          Koin y con su propia fábrica, así que se retira. `merge` conserva el resto de inicializadores
          —el de `lifecycle-process` entre ellos— (012 research.md D-420).
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <!--
          `singleTop`: tocar una notificación con la aplicación abierta reutiliza la actividad y llega
          por `onNewIntent`. Con `standard` se apilaría una segunda portada (012 research.md D-424).
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity,DiscouragedApi"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>